use lib 'lib';
use Test;
use Clu::TerminalUtilities;

# can't test either of these
# without stubbing out Terminal::Width.terminal-width
# Test::Mock only works on classes
# not sure how to do it
#
# is(remaining-term-width("x" x 10), 70);
# is(wrap-with-indent(5, "foo bar baz foopl"), "foo\n     bar\n     baz\n     foopl")



done-testing;
